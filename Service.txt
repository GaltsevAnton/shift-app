Service (...Service)

Зачем: бизнес-логика (правила приложения).

Пример: JwtService

・создаёт JWT токен

・читает JWT токен

Обычно Service:

・не зависит от HTTP (не знает про request/response)

・содержит логику “что делать”, “как делать”.

✅ Service = “мозги приложения”.